cartodb-node-app-boilerplate
==================================

Shows a map with the countries where your albums are available.

1. Rename ```lib/config.sample.js``` to ```lib/config.js``` and add your CartoDB credentials.
2. ```npm install```
3. ```node app.js```
4. ```open http://localhost:5000```
